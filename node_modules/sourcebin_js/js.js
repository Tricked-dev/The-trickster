const sourcebin = require('./index.js');

sourcebin.create([{
    name: `s‏`,
    content: `dd`,
    languageId: 'rage'
  }]).then(src => {
      console.log(`**[Link](${src.url}), [Short](${src.short}), [Raw](${src.files[0].raw})**`)
  })
    .catch(console.error);